extends ../layout

block content

    script( defer src="/javascripts/status.js")

    form.col-md-12
        .form-group
            label(for='statusTitle') Название
            input#statusTitle.form-control(placeholder='Введите название' value=status.statusTitle)
        .form-group
            #message.alert.alert-danger(style="display: none")

        #updateStatusButton.btn.btn-primary(data-status-id=status.statusID) Обновить